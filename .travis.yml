matrix:
  include:
    - language: python
      python: 3.6
      script:
        - nosetests
    - language: node_js
      node_js: node
      before_install:
        - cd frontend
      script:
        - yarn test
